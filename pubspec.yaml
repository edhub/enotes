name: enotes
description: "Timeline Kanban note-taking app for macOS."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.4
dependencies:
  flutter:
    sdk: flutter

  flutter_riverpod: ^3.3.1
  path_provider: ^2.1.5
  intl: ^0.20.2
  uuid: ^4.5.3
  sqlite3: ^3.3.1
  sqlite3_flutter_libs: ^0.6.0+eol
  file_selector: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
